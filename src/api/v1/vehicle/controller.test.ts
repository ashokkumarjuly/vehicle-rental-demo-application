describe('Controller :: Vehicle', () => {
    test('POST api/v1/vehicles/  Create vehicle', async () => {
        expect(2).toBe(2);
    });
    test('PUT api/v1/vehicles/UID  Update vehicle', async () => {
        expect(2).toBe(2);
    });
    test('GET api/v1/vehicles/UID  GET vehicle', async () => {
        expect(2).toBe(2);
    });
});
